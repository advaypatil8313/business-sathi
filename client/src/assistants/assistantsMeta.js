import { BarChart3, Megaphone, MessageCircle, Compass } from 'lucide-react';

// Frontend-only presentation metadata, keyed by the same assistant key the backend uses.
export const assistantIcons = {
  'business-analyst': BarChart3,
  'marketing-sathi': Megaphone,
  'customer-sathi': MessageCircle,
  'business-advisor': Compass,
};
